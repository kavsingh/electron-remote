{
	// base tsconfig for node targets
	"$schema": "https://json.schemastore.org/tsconfig",
	"extends": ["./tsconfig.base.json", "@tsconfig/node24/tsconfig.json"],
	"compilerOptions": { "types": ["node"] }
}
